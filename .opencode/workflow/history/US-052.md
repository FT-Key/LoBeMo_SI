# US-052 — Limpiar Código Muerto

## Estado: ✅ Done
## Prioridad: Baja
## Sprint: 1
## Branch: feat/US-052-limpieza-codigo-muerto
## PR: pendiente (→ dev)

## Problema
- Navbar legacy sin usar
- Schemas de documentos inconsistentes
- Paginación copy-pasteada en 8+ componentes
- Filtros repetidos

## Solución

### 1. Eliminar `src/components/navbar.tsx` (verificar importaciones)

### 2. Unificar schemas de documentos

### 3. Crear `Pagination`, `SearchInput`, `FilterSelect` compartidos

### 4. Eliminar imports muertos

## Archivos a crear
- `src/components/ui/pagination.tsx`
- `src/components/ui/search-input.tsx`
- `src/components/ui/filter-select.tsx`

## Archivos a eliminar
- `src/components/navbar.tsx`

## Archivos a modificar
- `src/shared/validation/documentos.ts`
- `src/app/api/documentos/route.ts`
- ~8 componentes de listado

## Dependencias
Ninguna

## Implementación (2026-09-08)

### Eliminado
- `src/components/navbar.tsx` — 0 imports en el codebase, componente legacy sin uso
- Import muerto `deleteFromR2, isR2Configured` en `src/app/api/documentos/route.ts`
  (se usan en `[id]/route.ts` y upload routes, no acá)
- 10× `type Pagination` locales → `PaginationInfo` compartido

### Creados (`src/components/ui/`)
- `pagination.tsx` — `PaginationInfo` + `initialPagination()` + `<Pagination>` (Anterior/Siguiente + "Mostrando X–Y de Z", `className` opcional para variante responsive)
- `search-input.tsx` — `<SearchInput>` (value/onChange/placeholder/className + `onKeyDown` para Enter-to-search)
- `filter-select.tsx` — `<FilterSelect>` (value/onChange/options/allLabel)

### Refactorizados (10 listados, sin cambios de comportamiento)
- `clientes-list`, `servicios-list`, `proyectos-list`, `propuestas-list`, `empleados-content`
- `soporte/ticket-list`, `pentesting/hallazgo-list`, `informes-auditoria-list`, `capacitaciones-list`, `audit-log-list`

### Schemas documentos (`src/shared/validation/documentos.ts`)
- Base `documentoBaseSchema` compartida (proyectoId, nombreArchivo, tipo, tareaId)
- `createDocumentoSchema` y `createDocumentoBase64Schema` extienden la base — validación idéntica

### Quality gates
- ✅ Typecheck: pasa
- ✅ Lint: 0 problemas en los 15 archivos tocados
- ✅ Tests: 59/59
- ✅ Build: compila (Next 16.2.9, 60/60 páginas)
- ⚠️ Pre-existentes fuera de scope (no tocados): `prisma/seed.ts` (2 any), `time-tracker.tsx` (set-state-in-effect + 3 unused), `proyecto-detalle.tsx` (`_doc`)
