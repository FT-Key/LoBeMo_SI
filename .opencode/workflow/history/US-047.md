# US-047 — Modo Oscuro/Claro

## Estado: ✅ Done
## Prioridad: Baja
## Sprint: 6
## PR: #84 → dev (pendiente de merge)

## Problema
El sistema es solo dark mode. Sin opción de light mode.

## Solución

### 1. Instalar `next-themes`

### 2. Crear `ThemeProvider` y envolver en `layout.tsx`

### 3. Separar CSS variables: `:root` (light) + `.dark` (dark)

### 4. Componente `ThemeToggle` en sidebar

## Archivos a crear
- `src/components/theme-provider.tsx`
- `src/components/theme-toggle.tsx`

## Archivos a modificar
- `src/app/layout.tsx`
- `src/app/globals.css`
- `src/components/admin/admin-sidebar.tsx`

## Dependencias
Ninguna

## Implementación

### 1. `next-themes@0.4.6` (attribute="class", defaultTheme="dark", enableSystem)
### 2. `src/components/theme-provider.tsx` + wrap en `layout.tsx` (se quitó `dark` hardcodeado, `suppressHydrationWarning`)
### 3. `globals.css`: `:root` = paleta clara, `.dark` = paleta oscura actual (sin cambios visuales por defecto)
### 4. `src/components/theme-toggle.tsx` (Sol/Luna, guard de montaje con `useSyncExternalStore`, soporte sidebar colapsado)
### 5. Toggle en sidebar: footer desktop + drawer móvil + header

## Quality Gates
- ✅ TypeScript (`tsc --noEmit`): sin errores
- ✅ Build (`npm run build`): exitoso
- ✅ Lint: 0 errores nuevos (3 errores + 6 warnings pre-existentes)
