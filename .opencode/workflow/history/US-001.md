# US-001: Autenticación y registro de empleados

## Card Info
- **Trello Card:** hYi3vqEW
- **Link:** https://trello.com/c/hYi3vqEW
- **Size:** M
- **Priority:** Must Have
- **Status:** In Progress

## Description
**Como** Gerente General, **quiero** iniciar sesión en el sistema y gestionar los empleados de LoBeMo **para** controlar quién accede al sistema y mantener actualizada la nómina del personal.

## Acceptance Criteria
- AC-01: El sistema debe permitir iniciar sesión con email y contraseña (Auth.js v5 con credentials provider).
- AC-02: El primer inicio debe crear al Gerente General como superadmin.
- AC-03: El Gerente General puede crear nuevos empleados con los 11 roles predefinidos.
- AC-04: El sistema debe rechazar emails duplicados al crear empleados.
- AC-05: El sistema debe encriptar las contraseñas con bcrypt.

## Plan

### Files to modify:
1. `prisma/schema.prisma` — Update User model to Empleado with roles/areas per domain model
2. `src/lib/prisma.ts` — Adjust import path

### Files to create:
3. `src/auth.ts` — Auth.js v5 config with credentials provider + bcrypt
4. `src/app/api/auth/[...nextauth]/route.ts` — Auth.js API route handler
5. `src/middleware.ts` — Route protection middleware
6. `src/app/(auth)/login/page.tsx` — Login page with email/password form
7. `src/app/(auth)/register/page.tsx` — First-time superadmin registration
8. `src/app/dashboard/page.tsx` — Post-login dashboard
9. `src/app/dashboard/layout.tsx` — Dashboard layout with nav
10. `src/app/empleados/page.tsx` — Employee list (GG only)
11. `src/app/empleados/nuevo/page.tsx` — Create employee form
12. `src/app/api/empleados/route.ts` — API for CRUD employees
13. `src/lib/auth-helpers.ts` — Auth helpers (getSession, requireAuth, isGerenteGeneral)

## Implementation

### Files created:
1. `src/auth.ts` — Auth.js v5 config with credentials provider + bcrypt
2. `src/app/api/auth/[...nextauth]/route.ts` — Auth.js API route handler
3. `src/middleware.ts` — Route protection middleware
4. `src/app/(auth)/login/page.tsx` — Login page
5. `src/app/(auth)/login/login-form.tsx` — Login form component
6. `src/app/(auth)/register/page.tsx` — Superadmin registration page
7. `src/app/(auth)/register/register-form.tsx` — Registration form
8. `src/app/dashboard/page.tsx` — Post-login dashboard
9. `src/app/empleados/page.tsx` — Employee list (GG only)
10. `src/app/empleados/nuevo/page.tsx` — Create employee page
11. `src/app/empleados/nuevo/form.tsx` — Create employee form
12. `src/app/api/empleados/route.ts` — API for CRUD employees
13. `src/lib/auth-helpers.ts` — Auth helpers (requireAuth, requireGerenteGeneral)
14. `src/types/next-auth.d.ts` — Auth.js type extensions for rol

### Files modified:
1. `prisma/schema.prisma` — Updated to full domain model (all 11 entities + enums)
2. `src/app/page.tsx` — Added conditional register link (hidden if superadmin exists)

### Key decisions:
- Used credentials provider with bcrypt (cost 12) per AC-05
- First registration uses `isInitialSetup` flag bypassing auth check
- Middleware checks cookie-based auth to avoid Prisma in edge runtime
- Types extended via next-auth.d.ts to include `rol` on session.user

## Quality Gates Results
### ✅ Build (TypeScript)
- Compiled successfully with 0 type errors
- All routes registered and optimized

### ✅ Lint
- ESLint passed with no errors

## Summary
US-001 implemented with full authentication flow (login/register) and employee management (list/create) accessible only to Gerente General.
