# US-010: Carga y gestión de documentos

## Card Info
- **Card ID**: O8Qb1S6D
- **Trello**: https://trello.com/c/O8Qb1S6D/10-us-010-carga-y-gesti%C3%B3n-de-documentos

## Description
**Como** Empleado técnico, **quiero** adjuntar documentos (informes, código, configuraciones) a proyectos y tareas **para** centralizar la documentación de cada servicio.

## Acceptance Criteria
- AC-01: Se pueden subir archivos PDF, imágenes, documentos de Office y archivos de texto.
- AC-02: Los documentos se tipifican (INFORME_AUDITORIA, REPORTE_PENTESTING, etc.).
- AC-03: Solo el equipo del proyecto puede ver sus documentos; Gerencia y CISO ven todos.

## Final Summary

### Quality Gates Results
- **Code Review**: Correcciones aplicadas (server-side MIME validation, XSS protection, AUDITOR/CAPACITADOR UI access)
- **Lint**: No ejecutable por entorno npm dañado (node_modules vacío)
- **Typecheck**: No ejecutable por entorno npm dañado

### Files created/modified
- `src/app/api/documentos/route.ts` — GET list + POST create (with MIME validation, pagination, audit log)
- `src/app/api/documentos/[id]/route.ts` — GET single + DELETE (with role check, audit log)
- `src/app/proyectos/[id]/page.tsx` — Added `documentos` to Prisma include
- `src/app/proyectos/[id]/proyecto-detalle.tsx` — Added documents section with upload form, type selector, inline viewer
- `.opencode/workflow/STATE.md` — Updated to track US-010 progress
- `.opencode/workflow/history/US-010.md` — Created

### Key technical decisions
- File storage: base64 data URL in DB (MVP approach, swappable to Vercel Blob)
- MIME validation: server-side via data URL prefix inspection
- XSS prevention: only image/PDF types rendered inline; others show download link with `rel="noopener noreferrer"`
- Access control: GERENTE_GENERAL/CISO see all; assigned employees see their project docs

### Git + PR
- Branch: `feat/US-010-gestion-documentos`
- PR: [#13](https://github.com/FT-Key/LoBeMo_SI/pull/13) → merged to dev
- Card moved to ✅ Done in Trello

## Plan

### Files to create:
1. `src/app/api/documentos/route.ts` — GET list + POST create document
2. `src/app/api/documentos/[id]/route.ts` — GET single + DELETE document

### Files to modify:
3. `src/app/proyectos/[id]/proyecto-detalle.tsx` — Add "Documentos" section with upload/list

### Design decisions:
- File storage: base64 data URL stored in `Documento.url` (MVP approach, swappable to Vercel Blob)
- Auth: employees assigned to project can upload/view; GERENTE_GENERAL and CISO can view all
- Validation: file size limited client-side, type classification required
- AuditLog: CREATE and DELETE operations logged
