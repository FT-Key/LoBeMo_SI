# US-050 — Tests Automatizados

## Estado: ✅ Done
## Prioridad: Baja
## Sprint: 6
## Rama: feat/US-050-tests → dev

## Problema
Sin tests automatizados. RNF-10 pide 80% cobertura.

## Solución (alcance de esta etapa: base + unitarios puros)

### 1. Infraestructura
- `vitest@^3` + `@vitest/coverage-v8@^3` como devDependencies (vitest 3 por compatibilidad con `@types/node@20` del proyecto; vitest 5 exige `@types/node ^22`)
- `vitest.config.ts` (nuevo): environment node, `include: src/**/*.test.ts`, alias `@` → `./src`, cobertura v8 sobre `src/lib` y `src/shared` (excluye tests y generated)
- Scripts en `package.json`: `test` (watch), `test:run`, `test:coverage`

### 2. Tests unitarios (54 tests, 7 archivos — sin DB ni red)
- `src/lib/utils.test.ts` (5): `cn()` — combinación, falsy, resolución tailwind, condicionales, arrays
- `src/lib/email-templates/layout.test.ts` (12): layout/header/card/field/button/label
- `src/lib/proyecto-codigo.test.ts` (5): formato `LBM-XXXX-XXXX`, prefijo sin acentos, reintento ante colisión, error tras 10 intentos (prisma mockeado con `vi.mock`), `formatCodigoDisplay`
- `src/shared/validation/auth.test.ts` (7): login/register schemas
- `src/shared/validation/clientes.test.ts` (9): CUIT con/sin guiones, CUIT inválido, razón social, sector, opcionales vacíos, update parcial
- `src/shared/validation/tareas.test.ts` (7): default MEDIA, título/proyectoId/prioridad, update estado
- `src/shared/validation/proyectos.test.ts` (9): create, transición, portal acceso/cambio clave

### 3. Fuera de alcance (siguiente etapa)
- Tests de integración API/DB (proyectos, clientes, auth): requieren BD de test + seed
- Cobertura 80% RNF-10: la base queda lista (`test:coverage`), falta ampliar suite

## Archivos a crear
- `vitest.config.ts`
- `src/**/*.test.ts` (7 archivos)

## Archivos a modificar
- `package.json` (+ `package-lock.json`)

## Dependencias
US-033 (RBAC) — ya en dev

## Quality Gates
- ✅ Tests (`npm run test:run`): 7 archivos, 54/54 en verde
- ✅ TypeScript (`npx tsc --noEmit`): sin errores
- ✅ Lint (`npx eslint` en archivos tocados): sin errores
- ✅ Build (`npm run build`): exitoso
