# US-067: Fix encoding, documentos nueva pestaña y hrefs

## Resumen
Fix de encoding corrupto en emails, apertura de documentos en nueva pestaña en portal de seguimiento, y reemplazo de `<a href>` por `<Link>` de Next.js para evitar recargas de página.

## Cambios Realizados

### 1. Fix encoding (caracteres corruptos UTF-8)
**Archivos afectados:**
- `src/app/api/proyectos/[id]/route.ts` — Líneas 112, 115, 117, 118
  - `Ã±` → `ñ`, `Ã³` → `ó`, `Ã©` → `é`
  - Corregía: asunto, título y subtítulo del email de "contraseña actualizada"
- `src/app/api/configuracion/route.ts` — Líneas 13, 37, 42
  - Corregía: mensajes de error de validación de configuración
- `src/app/api/asignaciones/route.ts` — Líneas 74, 94, 115
  - Corregía: mensajes de error de asignación de proyectos

### 2. Documentos abren en nueva pestaña
**Archivo:** `src/app/seguimiento/[codigo]/portal-content.tsx`
- Agregado `target="_blank" rel="noopener noreferrer"` al link de descarga de documentos en el portal de seguimiento
- Permite al cliente ver el documento sin salir de la página de seguimiento

### 3. Reemplazo de `<a href>` por `<Link>`
**Archivos modificados:**
- `src/app/seguimiento/page.tsx` — Línea 105
  - `<a href="/solicitar-acceso">` → `<Link href="/solicitar-acceso">`
- `src/components/modals/portal-login-modal.tsx` — Línea 131
  - `<a href="/solicitar-acceso">` → `<Link href="/solicitar-acceso">`
  - Agregado import de `Link` desde `next/link`

## Archivos No Modificados (correctos)
- `<Link href=...>` existentes en el proyecto (ya usan Next.js router)
- `<a href="mailto:...">` y `<a href="tel:...">` en email templates (HTML estático)
- `<a href=... download>` en `proyecto-detalle.tsx` (descargas con target="_blank")

## Testing
- Verificar que los emails de "contraseña actualizada" se ven bien con ñ y tildes
- Verificar que documentos en portal de seguimiento abren en nueva pestaña
- Verificar que links internos no recargan la página
