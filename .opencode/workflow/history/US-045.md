# US-045 — Gantt Simplificado

## Estado: ✅ Done
## Prioridad: Media
## Sprint: 5
## PR: #82 → dev (pendiente de merge)

## Problema
No hay visualización temporal de tareas y hitos.

## Solución

### 1. No requiere cambios en schema

### 2. Componente `GanttChart` (sin librería externa)
Timeline horizontal, barras por tarea, marcadores por hito.

### 3. Extender `GET /api/calendario` con datos de tareas

### 4. Página o pestaña en detalle de proyecto

## Archivos a crear
- `src/components/gantt/gantt-chart.tsx`
- `src/components/gantt/gantt-row.tsx`
- `src/app/proyectos/[id]/gantt/page.tsx`

## Archivos a modificar
- `src/app/api/calendario/route.ts`

## Dependencias
Ninguna

## Implementación

### 1. Componentes (sin librería externa, divs con % left/width)
- `src/components/gantt/gantt-chart.tsx` — Timeline horizontal: escala temporal adaptativa (ticks cada 1/3/7/14 días según rango), carril de hitos con diamantes, leyenda por estado, lista de hitos con fecha
- `src/components/gantt/gantt-row.tsx` — Barra de una tarea (inicio = createdAt, fin = fechaLimite o inicio; color por estado)

### 2. `GET /api/calendario` extendido con tareas
- Query `tarea` con fechaLimite no nula + eventos tipo `"tarea"` (titulo, fecha, estado, prioridad, proyecto)

### 3. Página + pestaña en detalle de proyecto
- `src/app/proyectos/[id]/gantt/page.tsx` — Página dedicada (Server Component, mismo patrón que metricas/page.tsx)
- `src/app/proyectos/[id]/page.tsx` — Link "Gantt" en header
- `src/app/proyectos/[id]/proyecto-detalle.tsx` — Tercer modo de vista (lista/kanban/gantt) con botón ChartGantt

## Quality Gates
- ✅ TypeScript (`tsc --noEmit`): sin errores
- ✅ Build (`npm run build`): exitoso, ruta `/proyectos/[id]/gantt` generada
- ✅ Lint: 0 errores nuevos (3 errores + 6 warnings pre-existentes en seed.ts, documentos/route.ts, time-tracker.tsx)
