# US-021: Landing page con identidad visual LoBeMo

## Card Info
- **Trello Card:** hCrnRlEZ
- **Link:** https://trello.com/c/hCrnRlEZ
- **Size:** M
- **Priority:** Must Have
- **Status:** In Progress

## Description
**Como** visitante del sistema, **quiero** ver una landing page profesional con la identidad visual de LoBeMo **para** tener una presentación atractiva de la empresa al acceder a la raíz del sitio.

## Acceptance Criteria
- AC-01: La página `/` debe aplicar el diseño del DESIGN_SYSTEM.md (dark mode, glassmorphism, geometría decorativa tipo Fortinet).
- AC-02: Debe mostrar: logo/nombre de LoBeMo, descripción del sistema, call-to-action para iniciar sesión.
- AC-03: Si no hay superadmin registrado, debe mostrar también enlace a "Primer inicio" (registro).
- AC-04: Debe incluir un footer con copyright y año dinámico.
- AC-05: Debe ser responsive (mobile-first).
- AC-06: Debe usar los tokens de color, tipografía y espaciado definidos en DESIGN_SYSTEM.md.

## Plan

### Files to create:
1. `src/components/landing/hero-section.tsx` — Hero with geometric background, logo, CTAs
2. `src/components/landing/services-section.tsx` — 6 glassmorphism service cards
3. `src/components/landing/stats-section.tsx` — 4 key indicators
4. `src/components/landing/features-section.tsx` — 3 differentiators
5. `src/components/landing/cta-section.tsx` — Final CTA
6. `src/components/landing/footer-section.tsx` — Footer with copyright
7. `src/components/landing/geometric-background.tsx` — Fortinet-inspired decorative geometry

### Files to modify:
8. `src/app/globals.css` — Add design system tokens (colors, fonts, glassmorphism)
9. `src/app/layout.tsx` — Add Plus Jakarta Sans + Inter + JetBrains Mono fonts, force dark mode
10. `src/app/page.tsx` — Compose all landing sections with conditional register link

### Implementation decisions:
- Dark mode as primary (`class="dark"` on `<html>`)
- CSS variables for all tokens in globals.css using `@theme` directive (Tailwind v4)
- Glassmorphism: `bg-surface-elevated/80 backdrop-blur-md border border-border/50`
- Geometric background: CSS pseudo-elements with `clip-path` for Fortinet-style shapes
- All sections responsive with mobile-first approach
- Services data as static array (no DB needed)

## Implementation

### Files created:
1. `src/components/landing/geometric-background.tsx` — Decorative geometry (diamonds, rays, gradients)
2. `src/components/landing/hero-section.tsx` — Full viewport hero with nav, CTAs, scroll indicator
3. `src/components/landing/services-section.tsx` — 6 service cards with glassmorphism
4. `src/components/landing/stats-section.tsx` — 4 stat indicators
5. `src/components/landing/features-section.tsx` — 3 differentiator cards with glow effects
6. `src/components/landing/cta-section.tsx` — Final CTA with gradient background
7. `src/components/landing/footer-section.tsx` — Footer with links, contact, dynamic copyright

### Files modified:
1. `src/app/globals.css` — Added all design system tokens (colors, radius, fonts) via `@theme`
2. `src/app/layout.tsx` — Switched to Plus Jakarta Sans (headings), Inter (body), JetBrains Mono (code); forced dark mode
3. `src/app/page.tsx` — Composed all 7 sections; conditional register link when no superadmin
4. `eslint.config.mjs` — Added `src/generated/**` to ignored paths

### Key decisions:
- Used CSS custom properties for all tokens with `@theme inline` for Tailwind v4 compatibility
- Fonts loaded via next/font/google with CSS variables for Tailwind consumption
- Geometric background uses CSS `clip-path` for Fortinet-inspired diamond/radar shapes
- `GeometricBackground` rendered as `fixed inset-0 -z-10` to cover all sections
- All interactive elements have `transition-all duration-200` per design system

## Quality Gates Results

### ✅ Build (TypeScript + Next.js)
- Compiled successfully with 0 type errors in 12s
- All routes registered (static: `/`, dynamic: dashboard, empleados, auth)

### ✅ Lint
- ESLint passed with no errors (added src/generated/ to ignore list for Prisma client)
- 1 pre-existing error in auth.ts fixed (Unexpected any → typed as User)

### ✅ Code Review
- All components follow design system tokens and patterns
- No hardcoded colors — all use CSS variables via Tailwind theme
- Dark mode is primary, applied at `<html>` level
- Responsive mobile-first with Tailwind breakpoints

## Summary
US-021 implemented with a full professional landing page featuring: geometric Fortinet-inspired background, hero section with CTAs, 6 glassmorphism service cards, 4 key stats, 3 differentiator cards with glow effects, CTA section with gradient, and responsive footer. All design system tokens applied. Conditional registration link shown when no superadmin exists.
