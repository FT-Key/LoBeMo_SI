# US-048 — Animaciones de Transición

## Estado: ✅ Done
## Prioridad: Baja
## Sprint: 6
## Rama: feat/US-048-transiciones → dev

## Problema
Transiciones instantáneas entre páginas sin feedback visual.

## Solución

### 1. Framer Motion ya instalado (usado en landing) — sin nuevas dependencias

### 2. Componente `PageTransition` (fade-in + slide-up sutil)
- `src/components/page-transition.tsx` (nuevo, client component)
- `motion.div` con `initial={{ opacity: 0, y: 12 }}`, `animate={{ opacity: 1, y: 0 }}`, `duration: 0.25, easeOut`
- `key={pathname}` vía `usePathname()` → re-ejecuta animación en cada cambio de ruta
- Respeta `prefers-reduced-motion` vía `useReducedMotion()` (render plano si activo)

### 3. Envolver en layout del admin
- `src/components/admin/admin-sidebar.tsx`: importa `PageTransition` y envuelve `{children}` del `<main>`

### 4. Animaciones sutiles en cards, modales, tabs
- Ya cubierto por utilidades existentes (`animate-fade-in`, `animate-slide-up`, `transition-all` en cards/sidebar); no se añadió bloat extra (KISS/YAGNI)

## Archivos creados
- `src/components/page-transition.tsx`

## Archivos modificados
- `src/components/admin/admin-sidebar.tsx`

## Quality gates
- ✅ TypeScript (`npx tsc --noEmit`): pasa sin errores
- ✅ Build (`npm run build`): pasa, todas las rutas compilan
- ✅ Lint: `src/components/page-transition.tsx` + `admin-sidebar.tsx` sin errores (repo tiene 3 errores pre-existentes en `time-tracker.tsx`, no tocados por esta US)

## Dependencias
Ninguna (framer-motion ^12 ya en package.json)
