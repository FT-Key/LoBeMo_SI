# US-040 — Filtros Guardados / Vistas Guardadas

## Estado: 📋 Backlog
## Prioridad: Alta
## Sprint: 3

## Problema
Los filtros se pierden al navegar away.

## Solución

### 1. Modelo Prisma `SavedFilter`
Campos: nombre, empleadoId, modulo, filtros (JSON).

### 2. CRUD `/api/saved-filters`

### 3. Hook `useSavedFilters(modulo)`

### 4. UI "Guardar vista" / "Vistas guardadas" en cada listado

## Archivos a crear
- Migración Prisma (`SavedFilter`)
- `src/app/api/saved-filters/route.ts`
- `src/hooks/use-saved-filters.ts`
- `src/components/ui/saved-filters.tsx`

## Archivos a modificar
- ~8 componentes de listado

## Dependencias
Ninguna
