# US-054: Fix API comentarios 500 + Seed roles/permisos

## Trello Card
- **ID**: 6aa03492475fd82aa45856f5
- **Board**: LoBeMo Seguridad Informática — MVP
- **List**: En proceso → Done
- **URL**: https://trello.com/c/6aa03492475fd82aa45856f5

## Resumen
Fix del error 500 en POST `/api/comentarios` y mejoras al seed para incluir empleado VENTAS, proyecto activo y comentarios de ejemplo.

## Problema
- POST `/api/comentarios` lanzaba error 500 cuando `session.user.id` no coincidía con ningún `Empleado.id` en la BD (FK violation de Prisma)
- El seed no tenía empleado con rol VENTAS
- Solo existía un proyecto CERRADO, sin proyectos activos para probar
- No había comentarios de ejemplo en el seed

## Cambios realizados

### 1. `src/app/api/comentarios/route.ts`
- Agregada validación de `autorId` antes del `prisma.comentario.create()` (línea 103-109)
- Agregado manejo específico de errores FK de Prisma en el catch (líneas 137-145)

### 2. `prisma/seed.ts`
- Agregado empleado VENTAS: Lucía Fernández (`lucia.fernandez@lobemo.com`)
- Agregado segundo cliente: TechStore S.R.L.
- Agregado proyecto activo: "TechStore - Soporte Técnico Premium" (estado: EN_EJECUCION)
- Agregadas asignaciones y tareas para el proyecto activo
- Agregados 3 comentarios de ejemplo en las tareas del proyecto activo
- Agregado `comentario.deleteMany()` en la sección de limpieza

## Archivos modificados
- `src/app/api/comentarios/route.ts` — fix FK validation
- `prisma/seed.ts` — seed data mejorado

## Decisiones técnicas
- Validación de empleado antes del create en vez de solo confiar en el catch de Prisma (defensa en profundidad)
- Manejo específico de `Foreign key constraint failed` para retornar 400 con mensaje descriptivo
- Segundo proyecto en estado EN_EJECUCION para testing de comentarios en vivo

## Quality Gates
- [x] Typecheck (`npx tsc --noEmit`): PASS
- [x] Build (`npm run build`): PASS
